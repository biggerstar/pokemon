---
alwaysApply: true
---


我调用 complete 和 errorComplete 必须关闭窗口，并且 complete 不需要重试， errorComplete需要根据最大重试次数进行重试

还有对应页面如果超时也需要关闭重试，直到超过重试次数

另外所有的状态除了 ( 超时和网络问题和手动停止日志 )
状态信息只能从前端传过来，不允许在后端进行更新


另外我普通任务在主进程超时 8 分钟关闭
如果路径到 chrome-error 页面的话直接关闭
在  about:blank 页面的话超时 20秒关闭

关闭后如果没超过最大重试次数的话需要进行重试
